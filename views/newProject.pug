extends layout

include mixins/crearProyecto

block contain
    .contenido-principal
        h1 #{nombrePagina}

        if errores 
            each error in errores 
                .alerta= error.texto

        +crearProyecto(proyecto)